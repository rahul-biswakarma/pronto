import { WebsiteSelector } from "@/libs/components/selector/website-selector";

export default function WebsitesPage() {
    return (
        <main className="min-h-screen bg-background">
            <WebsiteSelector />
        </main>
    );
}
